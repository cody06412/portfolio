import clsx from "clsx";

export const CardHeader = ({ children }) => {
    return (
        <div className="text-center text-2xl font-semibold mb-2 font-serif">
            {children}
        </div>
    );
};
